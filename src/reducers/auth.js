import React from 'react';

// INITIAL STATE

const initialState = {
  url: 'http://symply-book-me.herokuapp.com/',
};

const reducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
